SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# Backend API base URL — written into js/config.js by npm run config
# Local Spring Boot (must be running):  http://localhost:8080
# Firebase deploy / emulator without local backend: your Render URL, e.g.
#   https://jobtrack-backend.onrender.com
API_URL=http://localhost:8080
